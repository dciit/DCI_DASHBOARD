export interface ViApsPartMasterProps {
    model: string;
    partNameCode: string;
    partCode: string;
    wcno: string;
    partno: string;
    cm: string;
}
export interface ApsMainProps {
    sequnce: string;
    wcno: string;
    model: string;
    partno: string;
    plan: number;
    actual: number;
}